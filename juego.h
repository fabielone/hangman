#include "intro.h"

void juego()
{

    // mostar la primera pantalla

    intro();

    // codigo del juego
    // pedir al usuario que escoja una categoria
    // si no es valida mostrar mensaje y volver a preguntar
    // si escoje una categoria preguntarle si escojio la correct
    // si escogio salir pues salir del programa, de lo contrario continuar con el juegar
    // escoger una palabra al azar de la base de datos
    // borrar el menu
    // mostar animacion del hangman
    // mastrar abecedario
    // mostrar el marcador
    // mostrar los espacios de la palabra

    // pedirle al usuario que ponga una letra
    // verificar si la letra es parte de la palabra
    //  si si entonces ponerla en la poscicion en la q va
    //  y coloriar la letra en el abcdario de color verde
    // si no entonces dibujarle una parte al ahorcado
    // coloriar la letra en el abcdario de color rojo
    // actualizar el scoreboard
    // continuar hasta que la palabra se complete o el mono se dibuje por completo

    // al finalizar desaparecer el abcdeario
    // desaparecer la letras
    //  si gano correr animacion del hangman siendo liberado
    // si no correr animacion del hangman siendo ejecutado
    // si gano mostrar un mensaje de que gano y cuanto fue su puntacion
    // si no mostrar mensaje de que perdio y revelar la palabra.
    // si gano preguntarle al usuario por su nickname para el high score

    // preguntar al usuario de que si quiere volver a jugar o si quiere volver a jugar o si quiere salir
    // si si , guardar el high score y reiniciar el juego, guardar el nombre del jugador .

    // mastrar animacion de salida
}